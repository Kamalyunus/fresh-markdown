# Copy to ~/.env on the host that pulls the extract (fit.download_flc: the
# owner's pre-launch chain, and the weekly refresh the morning cron runs).
# Values are never committed and never appear in config.yaml or code.
REDSHIFT_HOST=
REDSHIFT_PORT=5439
REDSHIFT_DATABASE=
REDSHIFT_USERNAME=
REDSHIFT_PASSWORD=
