# GENERATED by the owner's `python3 -m ops.integration --sync` from the
# repository's config.yaml: the keys the two commands read, nothing else.
# The values are the owner's readings; do not edit them here -- the next
# sync overwrites this file.
meta:
  config_version: 1.0.0
data:
  max_window_hours: 120
reference_discount:
  MEAT: 0.25
  SIDE_DISH: 0.25
  _default: 0.3
features:
  table_dir: features
baseline_model:
  model_path: artifacts/baseline_model.txt
  feature_schema_path: artifacts/feature_schema.json
  calibration_factor_path: artifacts/calibration.json
dispersion:
  r_lookup_path: artifacts/r_lookup.json
posterior:
  path: artifacts/posterior.json
pricing:
  tier_step: 0.025
  entry_offsets:
  - -0.15
  - -0.1
  - -0.05
  - 0.0
  - 0.05
  negbin_max_k: 25
  demand_floor: 0.01
events:
  store_dir: events_store
