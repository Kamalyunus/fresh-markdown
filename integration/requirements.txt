# The floors the code is written against. The exact versions the sealed
# bundle was verified with are requirements.lock -- install THAT on the
# pricing host (ops.seal records library versions; a moved library is a
# red `artifact bundle` row in ops.status until a deliberate re-seal).
numpy>=1.26
pandas>=2.1
pyarrow>=14.0
scipy>=1.11
PyYAML>=6.0
lightgbm>=4.0
pytest>=7.4

# fit.download_flc only (step 0, and the weekly extract refresh on the cron
# host). Every other step reads the parquet it writes, so the pipeline runs
# without these; a host that never pulls from Redshift can leave them out.
psycopg2-binary>=2.9
python-dotenv>=1.0
