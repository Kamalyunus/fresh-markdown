# reports/hours/

`<day>T<hh>.json`, one per priced hour: shelves, entries, later hours,
decisions, rejections by reason, the feature tables read, the model and
config versions. Written by `price_hour.py --report`.
